name: Build for Appetize.io

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-simulator:
    runs-on: macos-latest

    steps:
      # 1️⃣ Checkout code
      - uses: actions/checkout@v4

      # 2️⃣ Set up Flutter (Force x64 for compatibility)
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          architecture: x64
          cache: true

      # 3️⃣ Create minimal Flutter project
      - name: Create minimal Flutter project
        run: |
          if [ ! -f pubspec.yaml ]; then
            echo "Creating minimal Flutter project..."
            flutter create --org com.example --project-name my_app .
          else
            echo "Flutter project found."
          fi

      # 4️⃣ Install dependencies
      - name: Install dependencies
        run: flutter pub get

      # 5️⃣ Build for Simulator (DEBUG Mode)
      # FIX: Changed --release to --debug because Flutter forbids release on simulators.
      - name: Build iOS Simulator App
        run: flutter build ios --debug --simulator --no-codesign

      # 6️⃣ Zip the App for Appetize
      # The output path for simulator builds is build/ios/iphonesimulator
      - name: Zip for Appetize
        run: |
          cd build/ios/iphonesimulator
          zip -r ../../../appetize-build.zip Runner.app

      # 7️⃣ Upload Artifact
      - name: Upload Simulator Build
        uses: actions/upload-artifact@v4
        with:
          name: appetize-build
          path: appetize-build.zip
